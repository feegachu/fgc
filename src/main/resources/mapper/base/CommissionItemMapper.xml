<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.susukkang.fgc.base.mapper.CommissionItemMapper">

    <select id="findEffectiveItems"
            resultType="com.susukkang.fgc.base.dto.CommissionItemResponse">
        SELECT item_code AS itemCode,
               item_name AS itemName,
               cashflow_type AS cashflowType,
               item_category AS itemCategory
          FROM fgc.commission_item
         WHERE active_yn = TRUE
           AND effective_from &lt;= #{asOf}
           AND (effective_to IS NULL OR effective_to &gt;= #{asOf})
         ORDER BY item_code
    </select>

</mapper>
